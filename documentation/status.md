# Статус проекта

Дата актуализации: 2026-06-10.

> Примечание о двух системах документации. В репозитории сосуществуют две параллельные
> системы: историческая `documentation/` (этот файл, ТЗ, as-is/to-be, backend/frontend doc)
> и рабочая `Tracker/` (`current.md`, `backlog.md`, `handoff.md`, `agent-context.md`).
> Живой, актуальный статус ведётся в `Tracker/` — для текущего состояния и порядка работ
> смотрите её в первую очередь. Файл `documentation/status.md` обновляется реже и служит
> укрупнённой исторической сводкой. Раздел «Состояние на 2026-06» ниже синхронизирует эту
> сводку с фактическим состоянием кода; более ранние разделы оставлены без изменений как
> исторический срез на 22.04.2026.

## Состояние на 2026-06

Сводка по завершённым с апреля направлениям (детали и проверки — в `Tracker/handoff.md`
и истории git).

### Спринты 1–3 (production-готовность и безопасность)

- **Спринт 1 — прод-блокеры:** исправлены блокирующие проблемы перед продом (миграции,
  корректная работа с денежными суммами, авторизация, логика регламента/gate).
- **Спринт 2 — целостность workflow:** добавлены guard'ы переходов, блокировки от гонок,
  аудит-трейл и soft-delete; история заявки строится на основе аудита; реализованы
  fan-out уведомления по переходам.
- **Спринт 3 — безопасность:**
  - валидируемые схемы и защита от mass-assignment на пользователях и справочниках;
  - object-level авторизация (закрыт класс уязвимостей IDOR);
  - усиление сессий: единый источник токена и отзыв (revocation) сессий;
  - явный маппинг «пользователь → организации», организационно-ограниченные остатки и
    назначение организаций пользователю в админке «Пользователи»;
  - поле ИНН в карточке организации (колонка + форма создания/редактирования).

### Архив уведомлений

- Добавлен персистентный архив уведомлений «Мои уведомления» (помимо живого fan-out по
  переходам workflow).

### Распознавание счетов (OCR) через evo-ai

- Реализовано распознавание прикреплённого счёта через платформу **evo-ai**
  (Dify-совместимый API): backend-сервис `app/services/ocr_service.py` и endpoint
  распознавания в `app/api/endpoints/requests.py`; настройки `EVOAI_*` в
  `app/core/config.py` (ключ API хранится вне репозитория).
- На фронтенде в форме заявки добавлена кнопка «Распознать счёт», которая автозаполняет
  поля заявки (контрагент в порядке «Название ОПФ», сумма, назначение платежа, дата),
  с автоподбором нашей организации-плательщика по ИНН покупателя.
- Распознавание устойчиво: всегда возвращается тип документа, есть ретраи и «спасение»
  невалидного JSON; нормализатор контрагента раскрывает полные организационно-правовые
  формы.
- Для регрессии добавлен backtest-харнесс `scripts/ocr_backtest.py`; последний прогон —
  **165/165** файлов распознаны без ошибок (`total=165 ok=165 errors=0`).

> Историческое ТЗ `documentation/OCR.md` описывает первоначальный пилот на GigaChat;
> фактическая реализация переведена на evo-ai.

---

## Текущее состояние (срез на 22.04.2026)

Система реализует единый реестр платежных заявок с ролевым workflow:

- инициатор создает и отправляет заявки;
- ФЭО согласует, отклоняет, уточняет, переносит, разрешает/отклоняет исключения и откладывает заявки;
- директор принимает решения по внебюджетным заявкам;
- кассир оплачивает согласованные заявки;
- бухгалтер ведет статус договора.

Актуальный бизнес-процесс описан в [current-workflow.md](current-workflow.md).

## Последние завершенные изменения

### 22.04.2026 — уточнение workflow вне бюджета

Сделано:

- добавлен промежуточный статус `MEMO_REQUIRED` для заявок вне бюджета;
- при выборе ФЭО `Бюджет = Нет` заявка сначала требует решения инициатора, а не уходит сразу директору;
- инициатор может `Обосновать`, `Отменить` или `Вернуть в черновик` такую заявку;
- после `Обосновать` заявка переходит в `PENDING_MEMO` и становится доступна директору;
- ФЭО/администратор могут вернуть `MEMO_REQUIRED` в черновик с новой датой, если платеж нужно перенести до решения директора;
- карточка заявки показывает обоснование вне бюджета отдельным полем.

Проверено:

- backend перезапущен перед финальным прогоном;
- `npm run test:api` — 6/6;
- `npm run test:regression` — 12/12.

### 22.04.2026 — карточка заявки, черновик UX

В работе:

- добавлен общий компонент карточки заявки для основного реестра и рабочего места казначея;
- модалка просмотра расширена и открывается ближе к верху экрана;
- данные заявки разложены по горизонтальным блокам `Реквизиты`, `Платеж`, `Статус и служебное`;
- текущие статусы и следующий ответственный видны в верхней части карточки;
- доступные действия в карточке переиспользуют существующую ролевую логику реестра;
- история заявки вынесена на отдельную вкладку `История`;
- события согласования и оплаты теперь записываются в историю заявки;
- форма создания/редактирования заявки перевёрстана в горизонтальные блоки `Реквизиты`, `Платеж`, `Регламент и служебное`.

Проверено:

- `npm run build`.

### 22.04.2026 — UX реестров и производительность модалок

Сделано:

- в основном реестре добавлено представление `По дням`: вкладки месяцев и вложенные вкладки дней внутри текущего отбора по датам оплаты;
- отбор по датам оплаты в основном реестре по умолчанию установлен на текущий месяц;
- представление `По дням` работает независимо от переключателя `Группировка`: внутри выбранного дня можно смотреть список или группированную таблицу;
- таблицы основного реестра и рабочего места казначея переведены на относительные веса колонок вместо пиксельных ширин;
- горизонтальная прокрутка и sticky-scrollbar в реестре удалены, таблицы подстраиваются под ширину экрана;
- табличный шрифт в реестрах приведен к `12px`;
- вложенные колонки разделены внутренней границей ячейки; в рабочем месте казначея обе строки вложенной ячейки отображаются одним цветом;
- модалки оптимизированы: скрытое содержимое размонтируется, таблицы и колонки мемоизированы, форма заявки больше не рендерится принудительно до открытия.

Проверено:

- `npm run build`;
- `npm run test:regression` — 12/12.

### 22.04.2026 — рабочее пространство казначея и Excel-экспорт

Сделано:

- добавлен экран `Рабочее пространство казначея` на `/cashier`;
- по умолчанию в рабочем месте казначея выбран текущий день оплаты;
- заявки разделены по вкладкам организаций, внутри вкладки таблица плоская;
- доступ к экрану вынесен в отдельное право `cashier_workspace_view`, назначено `CASHIER` и `FEO`;
- действие оплаты по-прежнему доступно только по праву `req_pay`;
- добавлена выгрузка текущего вида в Excel в основном реестре и рабочем месте казначея;
- добавлено право `req_export_excel`, назначено `CASHIER` и `FEO`;
- регрессионные тесты расширены сценариями рабочего места казначея.

Проверено:

- `npm run build`;
- `npm run test:api` — 6/6;
- `npm run test:e2e -- --headed` — 5/5;
- `npm run test:regression` — 11/11.

### 22.04.2026 — регрессионные тесты workflow

Сделано:

- добавлен постоянный Playwright-набор API/UI тестов в `frontend/tests`;
- добавлены команды `npm run test:api`, `npm run test:e2e`, `npm run test:regression`, `npm run test:regression:headed`;
- закрыт блок `P0 — Regression safety` в [backlog.md](backlog.md).

Проверено:

- `npm run build`;
- `npm run test:api` — 5/5;
- `npm run test:e2e -- --headed` — 3/3;
- `npm run test:regression` — 8/8.

### 22.04.2026 — preview регламента в форме заявки

Добавлено:

- `POST /requests/gate_preview`;
- общий backend helper проверки регламента;
- preview в модалке заявки после выбора организации, статьи ДДС и даты оплаты;
- учет матрицы ДДС для конкретного типа дня;
- совпадение preview с фактическим `submit`.

UI показывает:

- **«Заявка разрешена»**;
- **«Заявка требует дополнительного согласования»** с причиной.

### 21.04.2026 — унификация workflow-действий

Сделано:

- колонка **«Согласование»** стала read-only тегом;
- inline Select для изменения согласования удален;
- переходы вынесены в колонку **«Действия»**;
- основное действие показывается текстовой кнопкой;
- дополнительные действия доступны через меню `...`;
- `PENDING_GATE` отображается как **«Требует исключения»**;
- пользовательские действия: **«Разрешить исключение»**, **«Отклонить исключение»**;
- `SUSPENDED` отображается как **«Отложена»**;
- действие отложения теперь называется **«Отложить»**.

### 21.04.2026 — cleanup репозитория

Сделано:

- папка `docs/` удалена из индекса git;
- `docs/` добавлена в `.gitignore`;
- локальные счета и тестовые документы не попадают в репозиторий.

## Текущая статусная модель

| Технический статус | UI | Ответственный |
| --- | --- | --- |
| `DRAFT` | Черновик | Инициатор |
| `PENDING_GATE` | Требует исключения | ФЭО |
| `PENDING` | На согласовании | ФЭО |
| `MEMO_REQUIRED` | Требует обоснования | Инициатор |
| `PENDING_MEMO` | Вне бюджета | Директор |
| `APPROVED` | Согласовано | Кассир / ФЭО для отложения |
| `REJECTED` | Отклонено | Конечный статус согласования |
| `CLARIFICATION` | На уточнении | Инициатор |
| `POSTPONED` | Перенесено | Инициатор |
| `SUSPENDED` | Отложена | ФЭО |

## Текущие endpoints workflow

- `POST /requests/` — создать черновик;
- `POST /requests/gate_preview` — предварительно проверить регламент;
- `POST /requests/{id}/submit` — отправить заявку;
- `POST /requests/{id}/approve_gate` — разрешить исключение;
- `POST /requests/{id}/reject_gate` — отклонить исключение;
- `POST /requests/{id}/approve` — согласовать;
- `POST /requests/{id}/reject` — отклонить;
- `POST /requests/{id}/clarify` — вернуть на уточнение;
- `POST /requests/{id}/postpone` — перенести;
- `POST /requests/{id}/memo_reason` — обосновать внебюджетную заявку;
- `POST /requests/{id}/cancel_memo` — отменить внебюджетную заявку до передачи директору;
- `POST /requests/{id}/approve_memo` — утвердить вне бюджета;
- `POST /requests/{id}/reject_memo` — отклонить вне бюджета;
- `POST /requests/{id}/suspend` — отложить;
- `POST /requests/{id}/unsuspend` — вернуть на согласование;
- `POST /requests/{id}/pay` — оплатить.

Технические имена `gate_*` и `suspend/unsuspend` оставлены для совместимости. В UI используются бизнес-термины.

## Проверено

- `npm run build`;
- `python -m compileall app`;
- UI-first сценарии создания, исключения, согласования, оплаты, отложения и прав по ролям;
- backend preview совпадает с фактической логикой отправки.

## Backlog и следующий порядок работ

Рабочий порядок следующих задач ведется в [backlog.md](backlog.md).

Текущий приоритет:

1. `P1 — UX workflow`: улучшение карточки заявки, действий и подсказок.
2. `P2 — OCR`: автозаполнение заявки из счета после стабилизации тестов и UX.

`P0 — Regression safety` завершен 22.04.2026.
